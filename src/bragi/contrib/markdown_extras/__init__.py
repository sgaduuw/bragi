"""Small bundle of markdown-it-py extensions (footnotes, ...).

Each addition lives behind a single plugin so operators have one
switch to flip and one README section to point at. Adding a new
markdown-it plugin is a one-line append to the bundle in
`plugin.py` plus a paragraph in the README.
"""
