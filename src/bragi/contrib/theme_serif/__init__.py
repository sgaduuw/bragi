"""bragi.contrib.theme_serif: long-form reading theme.

Sibling to `theme_default`: same hookspec surface, same template
layout. Serif body type, sans-serif headings, warm paper-tone
backgrounds in light mode and warm dark gray in dark mode. Aimed
at essay-style sites where the post is the artifact.

CSS inlined; no `static_dir`.
"""
