"""SEO helpers: title, meta description, canonical URL, OG tags,
and JSON-LD generation. Used by both admin (preview) and delivery
(public render). Implementation lands as plugins ship.
"""

from __future__ import annotations
